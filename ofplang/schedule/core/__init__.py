"""Validator-agnostic infrastructure shared across ofplang.schedule.

Holds the position-tracking YAML wrapper, the diagnostics types, and small
identifier helpers, all reusable by the schema validators (and, later, by the
scheduler itself).
"""
