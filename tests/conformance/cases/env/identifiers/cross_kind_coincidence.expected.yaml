outcome: valid
warnings:
- {code: cross_kind_id_coincidence}
notes: >-
  A device and one of its spots share the id `reader_0`; spec §8.2 (warning — a
  spot is only ever referenced qualified, so it cannot be taken for a machine).
