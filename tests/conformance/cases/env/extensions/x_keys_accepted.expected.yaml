outcome: valid
notes: >
  Reserved `x-` extension keys are ignored at every closed mapping position
  (top level, time, device, transporter, transport, process map, mode); spec
  §9.4. Their values are not validated.
