outcome: invalid
match: exact
errors:
- {code: unknown_key}
notes: >-
  `inventories` is closed: it carries `levels` and `at`, and nothing else. The
  moment the levels are the levels of is spelled `at`; spec §6.10, §9.2.
